# Backend runtime env vars

APP_ENV=production
DEBUG=False
API_ENDPOINT=/api

# Postgres (external — Neon, Supabase, RDS va h.k.)
POSTGRES_DB=mlook
POSTGRES_HOST=db.example.com
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# Frontend origin(s) — vergul bilan ajratilgan
# Masalan: https://m-look.vercel.app,https://m-look.pages.dev
CORS_ORIGINS=http://localhost:3000

SECRET_KEY=change-me
